1 原先三轴振镜加工的流程为
 (0) 
（1） 进入目录E:/FiveAxis,执行npm start,弹出react窗口。
 (2)  cd laser_app_server_csharp 然后dotnet run 运行gRPC+tcp服务
 (3)  在杰普特激光器的软件中连接主机的串口，并设置外控出光
 (4)  现在运用三轴加工的流程是打开/Processing/dataGenerator.cs,在test1函数里手动修改加工需要用到的函数，点击test1后把数据帧传输给激光器和振镜控制卡，开始加工。
帮我分析一下原先的上位机通信方式。
我希望沿用原先的网络及串口设置，并在现在的项目里复刻这一通信流程(是否沿用dotnet看怎样方便)，并且最好能对接杰普特的软件(软件这台电脑上没有，先不实现，预留)
现在要求在菜单栏里加入 "连接" 一栏,实现功能 测试grpc通信、测试tcp通信、测试串口通信、连接，将通信及反馈信息输出到spdlog里。
2 在实现通信功能后我想在菜单加入一个“手动操作”的的功能，点击后实现一个界面，用图片assets/view/arrow.png组成四个指向上下左右的箭头，中间放一个assets/view/stop.png，先不用实现具体功能。